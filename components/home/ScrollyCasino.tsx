"use client";
import { motion,useScroll,useTransform } from "framer-motion";
import { useRef } from "react";
export function ScrollyCasino(){const ref=useRef<HTMLDivElement>(null);const {scrollYProgress}=useScroll({target:ref,offset:["start end","end start"]});const y=useTransform(scrollYProgress,[0,1],[80,-80]);return <section ref={ref} className="relative overflow-hidden rounded-3xl border border-[#2a1d19] bg-gradient-to-b from-[#080808] to-[#280d12] p-10"><motion.div style={{y}} className="absolute right-8 top-8 text-7xl opacity-20">🪙</motion.div><motion.div style={{y}} className="absolute left-12 top-28 text-6xl opacity-20">🎴</motion.div><div className="relative max-w-2xl space-y-4"><h2 className="display-title text-4xl">Enter a hidden room of risk design.</h2><p className="text-[#bfaf91]">Wallet simulation, fake bet history, demo RNG, and responsible-play controls blend into one cinematic flow.</p><p className="text-[#bfaf91]">No real money. No deposits. Pure portfolio simulation.</p></div></section>;}
